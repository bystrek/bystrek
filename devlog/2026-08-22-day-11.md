# Day Eleven — bystrek.dev

**Date:** 2026-08-22
**Goal:** Ship the chat page — the last piece of item 5 in `whats-next.md` — plus a small user profile page (name + avatar) added to today's scope. Wire a routed `/chat` page in `ui` to the already-live `POST /chat` SSE backend, streaming Claude's reply into a message list; add a `/profile` page for editing first/last name and an avatar.

## What we decided

**Profile: reuse `better-auth`'s own `update-user` endpoint and `image` field, don't hand-roll a controller.** Original ask was a new `users` column (base64 picture) + a new custom update endpoint + a UI page. Checked the actually-installed `better-auth` (v1.7.1) first: `image` is already a core field its built-in `POST /api/auth/update-user` endpoint accepts and writes straight to the user row (we just don't have the column yet), and `user.additionalFields` in `auth.config.ts` is the declared way to add more self-editable columns (`firstName`/`lastName`) — once declared, the same endpoint accepts them in the same batched call, and `get-session` returns them automatically, no new GET endpoint needed either. So the only new backend work is three nullable `users` columns (`image`, `first_name`, `last_name`) and an `additionalFields` declaration — no `ProfileController`, no new module. Same reasoning as picking Caddy over nginx on day 10: reuse a tool already fully wired into this stack over hand-rolling the standard-shaped equivalent. Storage stays plain `text` (a data-URI string), not `bytea` — going through better-auth's generic field path means it has to be a string type regardless, and a profile photo is the same sensitivity class as the already-plaintext `name`/`email` columns, so tier-2 field encryption doesn't apply here.

**Avatar gets downscaled/re-encoded client-side before it's sent.** A raw phone photo can be several MB; canvas-resize to a small square (~256px) and re-encode as JPEG before base64-encoding, so a typical upload stays a few tens of KB rather than blowing up the request body and the DB row. `name` (the existing single-field core column, read elsewhere for "Signed in as…") gets recomputed as `` `${firstName} ${lastName}`.trim() `` and sent in the same batched `update-user` call, so it never drifts out of sync with the new split fields.

**`name` is derived one-way from `firstName`/`lastName`, never the other way around.** First pass considered splitting the existing `name` value back into two inputs for editing (naive whitespace split) to avoid adding new columns at all — rejected once raised: a single-field split doesn't round-trip for anyone with multiple first or last names, so the canonical editable source has to be `firstName`/`lastName` themselves, with `name` as a pure write-side derivation kept in sync on every save. The only wrinkle: a user's very first profile visit shows blank first/last name inputs even though `name` already has a value from invite/seed — acceptable for a household this size, and both fields are required before submit so nothing gets silently blanked out.

**Add `GET /chat/history` today, not defer it.** The backend only exposes `POST /chat` right now — there's no way to load prior turns. The whole reason `messages` is a single continuous thread (not per-conversation, see day 9) was so history persists across sessions; a chat page that goes blank on every refresh undercuts that. The addition is small — reuse `ChatService.loadRecentMessages`'s existing decrypt path, collapse each row to plain text, drop turns that are pure `tool_use`/`tool_result` (no tool-result rendering exists yet, matches the "text only for v1" decision from day 9).

**Split into 4 PRs, each its own session — not chained through in one sitting.** Backend/frontend split per feature, mirroring how this repo already shipped chat's backend (#4) and the Angular rewrite (#5) as separate PRs: PR1 profile-backend → PR2 profile-frontend (depends on PR1), PR3 chat-backend → PR4 chat-frontend (depends on PR3). Each PR gets picked up cold in a fresh session — this devlog entry is the plan each of those sessions works from; a "What we actually built" writeup gets appended per PR as it lands, not all at once. Docs close out afterward as direct-to-main commits (no PR), matching how day 10 closed out (`932338b`).

## Plan

### PR1 — Profile: backend

**Scope:** `users` table gains `image`, `first_name`, `last_name` (all nullable `text`); `auth.config.ts` declares the two new fields as `additionalFields`.

**Files:**
- `api/src/db/schema.ts` — add the three columns to the `users` table definition (`image` is a bare column, no special handling — better-auth treats it as core).
- `api/drizzle/` — `bun run db:generate` for the migration, `bun run db:migrate` to apply locally.
- `api/src/auth/auth.config.ts` — add:
  ```ts
  user: {
    additionalFields: {
      firstName: { type: 'string', required: false, input: true },
      lastName: { type: 'string', required: false, input: true },
    },
  },
  ```
- No new controller/module — `POST /api/auth/update-user` and `GET /api/auth/get-session` are already mounted (better-auth is mounted wholesale) and already bearer-gated by better-auth's own session middleware.

**Tests:** new `api/test/integration/profile.integration.spec.ts`, following the `withRollback` + `testDb` + `signUpTestUser` pattern used in `household-endpoint.integration.spec.ts`. No existing test hits a better-auth core route over HTTP (all existing supertest calls target this app's own controllers; `signUpTestUser` calls better-auth in-process), so this test issues a real `supertest` request against `/api/auth/update-user` with the test user's bearer token, then asserts `{ firstName, lastName, image, name }` round-trip through `GET /api/auth/get-session`.

**Verification:** `bun test src` and `bun test test/integration` both green; manual curl against `/api/auth/update-user` with a real bearer token as a sanity check before writing the automated test.

**Built as planned, plus one addition the integration test surfaced: `/api/auth/update-user` and `/api/auth/get-session` need to be mounted by hand in a test's Nest app instance** — `main.ts` mounts better-auth's raw handler (`app.use('/api/auth', toNodeHandler(auth))`, ahead of Nest's own JSON body parser) only in `bootstrap()`, which `Test.createTestingModule(...).createNestApplication()` never runs. Every existing test either hits this app's own controllers or calls `auth.api.*` in-process (`signUpTestUser`), so this is the first test to need the real HTTP route — `profile.integration.spec.ts` replicates `main.ts`'s mount (`bodyParser: false`, mount auth, then `express.json()`) itself. Migration, schema columns, and `additionalFields` landed exactly as scoped. Manual curl against the running local `api` (throwaway household/user, cleaned up after) confirmed `name`/`firstName`/`lastName`/`image` all round-trip through the real server, not just the test transaction.

Ran into the `bun run lint`/`--fix` mess described in the formatting-unification continuation below while sanity-checking this PR — PR1's work went into a stash and came back out untouched once that got sorted; nothing here was affected by it.

**Verification, actually run:** `bun test src` (9 pass) and `bun test test/integration` (19 pass, all existing suites plus the two new profile cases) green; `bun run build` and `bun run format:check` green; manual curl round-trip against a live local `api` confirmed above.

### PR2 — Profile: frontend

**Depends on PR1 merged** (needs the columns/fields to exist against a running local `api`).

**Scope:** a `/profile` page — first/last name inputs + a file input for the avatar — wired to better-auth's `update-user`/`get-session` endpoints directly via `AuthService` (no separate profile service needed, matching how `AuthService` already calls other better-auth endpoints directly).

**Files:**
- `ui/src/app/core/auth/auth.service.ts` — widen `Session['user']` to include `firstName`, `lastName`, `image` (nullable strings); add `updateProfile(firstName, lastName, image)` that POSTs `{ name: \`${firstName} ${lastName}\`.trim(), firstName, lastName, image }` to `${environment.apiUrl}/api/auth/update-user`, then re-runs `initSession()` to refresh the displayed session (`update-user` only returns `{ status: true }`).
- `ui/src/app/features/profile/` (new): `profile.ts`/`.html`/`.css`. Form fields as signals (matching `main.ts`'s `inviteName`/`inviteEmail` pattern); first/last name both `required` (matches the invite form, prevents an accidental blank overwrite of `name` before either field has ever been set). File input's `change` handler draws the picked image to an off-screen `<canvas>`, downscales to a small square (~256px), and calls `toDataURL('image/jpeg', 0.8)` for the base64 payload.
- `ui/src/app/app.routes.ts` — add `/profile` (`authGuard`, lazy-loaded like the others).
- `ui/src/app/features/main/main.html` — link to `/profile`.

**Tests:**
- `auth.service.spec.ts` — extend with cases for `updateProfile`, following its existing `HttpTestingController` pattern.
- A unit test for the canvas-downscale helper if extracted as a standalone function.
- `ui/e2e/profile.spec.ts` (new) — fill first/last name, attach a small fixture image, submit, assert the `update-user` request body and that the displayed name/avatar refresh. Mock the HTTP call via `route.fulfill`, matching how `subscribe.spec.ts`/`household.spec.ts` already mock their backend calls.

**Verification:** `bun run build`, `bun run test`, `bun run test:e2e` all green; manual click-through in a browser against the real local `api` from PR1 to confirm the avatar round-trips and downscaling actually shrinks a real phone-sized photo.

### PR3 — Chat: backend (`GET /chat/history`)

**Scope:** expose the persisted thread as a plain-text list for the frontend to load on open.

**Files:**
- `api/src/chat/chat.service.ts` — extract a pure function that collapses decrypted `MessageParam[]` rows into `{ role, text }[]`: concatenates `text`-type content blocks per turn, drops any turn with no resulting text (pure `tool_use`/`tool_result` turns — no per-tool rendering exists yet, matches the "text only for v1" call from day 9).
- `api/src/chat/chat.controller.ts` — add `@Get()` handler, `@UseGuards(AuthGuard)`, reuses the existing `loadRecentMessages` decrypt/window logic, feeding its output through the new collapse function.

**Tests:** unit test the collapse function directly under `api/src/chat/` (co-located `*.spec.ts`, pure logic, no DB) — cases: plain text turn, tool_use-only turn (dropped), mixed content, empty history. Optionally extend `chat.integration.spec.ts` with one case hitting the new `GET` route end-to-end.

**Verification:** `bun test src` and `bun test test/integration` green; manual curl against a real seeded thread to eyeball the response shape.

### PR4 — Chat: frontend

**Depends on PR3 merged.**

**Scope:** the chat page itself — message list, input, SSE streaming via `HttpClient`.

**Files:**
- `ui/src/app/core/chat/chat.service.ts` (new): signals `messages`, `sending`, `error` (same shape as `PushService`/`HouseholdService`). `loadHistory()` calls `GET /chat/history` on page load. `send(text)` posts to `POST /chat` via `HttpClient` with `observe: 'events'`, `responseType: 'text'`, `reportProgress: true` — the XHR backend (`withXhr()`, already configured) exposes growing `partialText` on `DownloadProgress` events, so SSE deltas can be read incrementally without a raw `fetch()`. Parse complete `data: {...}\n\n` frames out of the accumulated text as they arrive; append each `delta` to the trailing assistant message. Outgoing sends route through a `Subject` + `switchMap` (per the architecture doc's stated reason for RxJS here) to cancel an in-flight stream if superseded, even though the UI also disables input while `sending()` is true.
- `ui/src/app/features/chat/` (new): message list (user/assistant bubbles), auto-scroll on new content, input + send button disabled while `sending()`. Loads history in `ngOnInit`.
- `ui/src/app/app.routes.ts` — add `/chat` (`authGuard`, lazy-loaded).
- `ui/src/app/features/main/main.html` — replace the "Chat is offline for now" placeholder with a real link to `/chat`.

**Tests:**
- Vitest for `ChatService` via `HttpTestingController`, simulating staged `DownloadProgress` events with growing `partialText` plus a final `Response` — same pattern as `auth.service.spec.ts`.
- `ui/e2e/chat.spec.ts` (new) — mock `GET /chat/history` (empty) and `POST /chat` (small fake SSE body via `route.fulfill`), assert: typed message appears, streamed reply renders, input/button disabled mid-stream and re-enabled after.

**Verification:** `bun run build`, `bun run test`, `bun run test:e2e` all green; manual click-through against the real local `api` (real Anthropic call) to confirm actual streaming renders correctly, not just the mocked e2e path.

### After all 4 land: docs close-out (no PR — direct-to-main commits)

Matches how day 10 closed out (commit `932338b`, not a PR merge):
- `docs/whats-next.md`: mark chat UI done (item 5); add the profile page as a done item.
- `docs/architecture.md`'s "Frontend & chat" section + README status section: update together now that the SvelteKit → Angular migration and chat both fully land (per `frontend-migration.md`'s own note to do these together).
- This file: final "Current state" close-out summary.

## Current state

Plan only, split into 4 PRs above — nothing implemented yet. Each PR starts fresh in its own session; PR1 (profile backend) is first up.

## Continuation: formatting unification (2nd session, same day)

### What we decided

**PR1 (profile backend) work got stashed mid-session to fix a tooling gap first.** While sanity-checking PR1 with `bun run lint` (api's script bundles `eslint --fix`), the autofix reformatted three unrelated files (`auth.guard.spec.ts`, `household.controller.ts`, `test/integration/support/auth.ts`) that weren't already prettier-clean on `main`. That's a symptom of a real gap, not a one-off: `api` has prettier wired through `eslint-plugin-prettier` but nothing ever runs it automatically (no pre-commit hook, no CI step) — it only fires when someone happens to run `bun run lint`. `ui` has prettier installed (Angular CLI's default scaffolding) but literally nothing invokes it — no `format` script, no eslint at all. Decided to fix this now rather than let it keep silently corrupting unrelated diffs in future sessions. PR1's changes (schema/auth-config/migration/integration test) are intact in `git stash@{0}`, untouched by this work.

**One root `.prettierrc`, not two per-project ones.** `api/.prettierrc` (`singleQuote`, `trailingComma: all`) and `ui/.prettierrc` (`singleQuote`, `printWidth: 100`, an `angular` parser override for `*.html`) already agreed on `singleQuote` but diverged on `printWidth`/`trailingComma` for no real reason — neither project's code style demands a difference. Deleting both in favor of one root config Prettier resolves by walking up from each file (no monorepo/workspace tooling needed, this isn't a workspace repo) gives one answer to "how is this file formatted" regardless of which project it's in. Kept the `*.html` → `angular` parser override in the shared config; it's a no-op for `api`, which has no `.html` files.

**Checked Biome, dprint, and oxfmt (oxc project) before committing to Prettier — none of them work for this repo today.** Biome's HTML support is experimental and disabled by default; Angular's control-flow syntax (`@if`/`@for`/`@defer`) isn't formatted yet (open issue). dprint's Angular support is a third-party plugin (`markup_fmt`, not from dprint or Angular) with undocumented file-detection behavior — not confidence-inspiring. oxfmt is the real contender: 100% Prettier JS/TS conformance claimed, explicit Angular template/style support, ~30x faster, already adopted by Vue.js and Turborepo — but it detects Angular templates by filename, only treating `*.component.html` as Angular. Our templates (`app.html`, `main.html`, `login.html`, `reset-password.html`) follow the newer Angular CLI convention that dropped the `.component.` suffix, so oxfmt would parse them as plain HTML and mangle Angular syntax. Its own compatibility table flags this ("`.html` requires config override") but the override doesn't exist yet — tracked in `oxc-project/oxc#17852`, open, unassigned, no PR. Revisit oxfmt once that issue ships; Prettier is the only one of the three with confirmed working Angular support that matches how this project actually names its files.

**CI format:check blocks the pipeline, not just reports.** Chose to fail `test-api`/`test-ui` on a formatting violation (same severity as a failing test) rather than a non-blocking report step. The alternative (report-only, grace period) risks exactly the kind of silent drift that caused this detour in the first place, and the fix is always a single `prettier --write` away — low friction to require it up front.

**Enforced via existing CI jobs, not a new pre-commit hook.** `test-api`/`test-ui` already run on every push/PR; adding a `format:check` step there reuses infrastructure that's already gating merges (per the day-10 "reuse a tool already in the stack" reasoning). No `husky`/`lint-staged` — nothing in this repo uses git hooks today, and CI is already the enforcement point for tests, so it's the enforcement point for formatting too.

### What we actually built

One root `.prettierrc` (`singleQuote`, `trailingComma: all`, `printWidth: 100`, `*.html` → `angular` parser override), replacing `api/.prettierrc` and `ui/.prettierrc`. Added `format`/`format:check` scripts to `ui/package.json` (it had neither before) and a `format:check` script to `api/package.json` (it already had `format`, which now also covers root-level `.ts` files like `drizzle.config.ts`). Reformatted both codebases under the unified config — pure whitespace/line-wrap changes throughout, spot-checked several diffs by hand to confirm nothing semantic moved. Added a `bun run format:check` step to both `test-api` and `test-ui` jobs in `.github/workflows/deploy.yml`, ahead of the existing test steps, so a misformatted file fails CI the same as a failing test.

### Problems hit along the way (and the actual fixes)

`bun run lint` in `api` bundles `eslint --fix` — running it (twice, once before and once after an interleaved `git stash`, while sanity-checking the formatting change) auto-fixed three unrelated files (`auth.guard.spec.ts`, `household.controller.ts`, `test/integration/support/auth.ts`) that weren't prettier-clean on `main` yet. This is what triggered the whole detour in the first place. Recovered by diffing the stash against `HEAD` (`git diff HEAD stash@{0} --stat`) to see exactly which files it touched, `git checkout --` the stray ones back to `HEAD`, and `git checkout stash@{0} -- <files>` to pull just PR1's real changes back into the working tree, then re-stashed PR1's work as one clean labeled entry (`git stash push -u -m "PR1 profile-backend: ..."`) before starting this PR. Ran into it a second time mid-verification — `bun run lint` silently reintroduced an unrelated fix (`(await x()) as Response` → `await x()`, an eslint `no-unnecessary-type-assertion` autofix, not a prettier formatting decision) into `support/auth.ts`. Caught it by re-running a bare `bunx prettier --write` on that file, which reported it "unchanged," proving the earlier diff wasn't formatting at all; reverted it and re-ran a full idempotent `prettier --write` pass across `api` (all files reported "unchanged") to confirm nothing else had been silently mixed in. Takeaway: don't run `bun run lint` (or any `--fix`-bundled script) as a "just checking" step on someone else's/older files — its autofix isn't scoped to intent, only to whatever's on disk. Verifying eslint's prettier integration should have been a plain `prettier --check`/`format:check`, not the fix-on-write lint script.

Also evaluated Biome, dprint, and oxfmt as faster alternatives before committing to Prettier (prompted by a direct question, not part of the original plan) — see "What we decided" above for why all three lose to Prettier today, specifically because this project's Angular templates (`app.html`, not `*.component.html`) hit an open, unfixed oxfmt bug.

`ui/node_modules` had never been installed in this environment — `bun install` was needed before `format:check`/`build`/`test` would run at all.

### Current state

Formatting unified and enforced. `api format:check`/`test`/`test:integration`/`build` all green; `ui format:check`/`build`/`test` (unit) all green — `test:e2e` not run (pure formatting change, no logic touched, already covered by build + unit tests). Not yet committed — this is its own PR per the plan above, separate from PR1. PR1 (profile backend)'s actual work is untouched, sitting in `git stash@{0}` ("PR1 profile-backend: ..."), ready to resume once this lands.

## Continuation: chat backend, PR3 (3rd session, same day)

### What we decided

**PR3 (chat backend) next, ahead of PR2 (profile frontend).** PR1 (profile backend) and the formatting-unification PR both landed and merged (`#7`, `#6`) since the last session. The original plan ordered PR2 before PR3, but PR3 has no dependency on PR2 — profile frontend and chat backend are independent slices — so doing chat backend next isn't a reordering that costs anything; PR2 just picks up whenever it's next up.

**`GET /chat/history` returns `{ role, text }[]`, built from a pure `toHistoryTurns` collapse function, exactly as PR3 originally scoped it.** `ChatService` gained a public `getHistory(userId)` that reuses the existing (private) `loadRecentMessages` decrypt/window path and feeds it through `toHistoryTurns`. `collapseTurnText` handles both storage shapes already in the `messages` table: a bare string (user turns) and a content-block array (assistant turns, which may mix `text` with `tool_use`); a turn that collapses to no text at all (pure `tool_use`/`tool_result`) is dropped, matching the "text only for v1" call from day 9.

**Found and fixed a real ordering bug while writing the history test, not part of the original PR3 scope.** `messages.createdAt` used `defaultNow()` — Postgres `now()`, which returns *transaction* start time, frozen across every statement in that transaction. `loadRecentMessages` sorts by `createdAt` alone with no tie-break; a single `ChatService.reply()` call persists several rows (user turn, assistant turn, and more on a tool loop) in quick succession, and if those inserts ever land in one transaction, the sort order is undefined and history can come back scrambled — first surfaced by the new `GET /chat/history` integration test flipping role order under the rolled-back test transaction, but a genuine latent risk for `POST /chat` context too, not a test-only artifact. Fixed at the root — swapped `messages.createdAt`'s default from `defaultNow()` to `clock_timestamp()` (real per-statement wall time), rather than adding a tie-break column or an `ORDER BY id` workaround, since the actual invariant that matters is "rows are ordered by when they were actually written," which `clock_timestamp()` gives directly. Left every other table's `createdAt` on `defaultNow()` — `messages` is the only table where multiple rows land in rapid succession as one logical unit and their relative order is read back out, so it's the only one where the distinction matters.

### What we actually built

`GET /chat/history` (`api/src/chat/chat.controller.ts`), `AuthGuard`-gated, calling `ChatService.getHistory`. `chat.service.ts` gained `getHistory`, plus exported `collapseTurnText`/`toHistoryTurns` and a `ChatHistoryTurn` type. New `api/src/chat/chat.service.spec.ts` (pure unit tests, no DB): plain-string turn, multi-block text turn, tool-use-only turn (dropped), mixed text+tool_result turn, empty history, and order-preserving collapse over a mixed sequence. Extended `chat.integration.spec.ts` with a 401-without-token case and an end-to-end case that runs a tool-call turn then asserts the returned history is exactly the two text turns in the right order. `api/src/db/schema.ts`: `messages.createdAt` default changed to `clock_timestamp()`; migration `0005_glossy_gorilla_man.sql` generated and applied locally.

### Problems hit along the way (and the actual fixes)

The new history integration test failed on order (`assistant` before `user`, reversed from insertion) the first time it ran — root-caused to the `defaultNow()` transaction-time issue above rather than a bug in `toHistoryTurns` itself; fixed by moving the column default to `clock_timestamp()` (see "What we decided"), not by adding a sort workaround in the query.

`tsc` rejected `toHistoryTurns`'s naive `{ role: message.role, text }` construction: `Anthropic.MessageParam['role']` is typed `'user' | 'assistant' | 'system'` in the installed SDK version, wider than this app's own `Role` (`'user' | 'assistant'`, matching the DB enum — `persist()` never writes `'system'`). Narrowed with an `as Role` cast at the mapping site rather than widening `ChatHistoryTurn` to carry a `'system'` case that can never actually occur.

### Current state

`api format:check`/`test`/`test:integration`/`build` all green. Not yet committed — PR3, same one-PR-per-session pattern as the rest of this plan. PR2 (profile frontend) and PR4 (chat frontend, depends on this) still queued.

## Continuation: profile frontend, PR2 (4th session, same day)

### What we decided

**Built as scoped, one deviation: `.link` styling widened from `button.link` to a bare `.link` selector.** The plan's "Edit profile" link from `main.html` to `/profile` needs `routerLink`, and an `<a>` can't reuse a button-scoped CSS rule — rather than duplicate the link's look under a second selector, widened `styles.css`'s existing rule to `.link` (adding `border: none; cursor: pointer;` so it still behaves as a button where used) so both the anchor and the existing "Sign out"/"Forgot password?" buttons share one rule.

**Skipped a jsdom unit test for `downscaleImage`, covered it via the real-browser e2e test instead.** The plan flagged this as conditional ("if extracted as a standalone function"). It was extracted (`profile/downscale-image.ts`), but jsdom has no `<canvas>` 2D context or real `Image` decoding, so a vitest unit test would need a canvas-mocking dependency this repo doesn't have, just to assert against fake pixel data. Playwright's WebKit *does* have a real canvas, and `e2e/profile.spec.ts` already exercises the exact path end to end — attaches a real fixture PNG, waits for the redrawn `<img class="avatar">`, submits, and asserts the POST body's `image` field is an actual `data:image/jpeg;base64,...` string. That's a stronger guarantee than a mocked unit test would give for this specific function, so no vitest case was added for it.

**Manual click-through against a real local `api` (the plan's last verification step) wasn't run this session** — no local Postgres/Anthropic-key environment was up. Everything else in the plan's verification list ran and passed: `bun run build`, `bun run test` (7/7, including two new `updateProfile` cases), `bun run test:e2e` (7/7, including the new profile flow), `bun run format:check`. The e2e test mocks `update-user`/`get-session`/`household` the same way `household.spec.ts` already does, so the request/response contract is exercised, just not against the real better-auth route PR1 added. Worth a real round-trip before this ships, not blocking for the PR itself.

**Signal Forms migration deferred to its own PR, not folded into this one.** Angular 22 made Signal Forms (`form()`/`field()`, declarative validation, `[field]` binding) stable, and this new `profile` form was flagged as a candidate. But `login`, `main`'s invite form, and `reset-password` all already use the same manual-`signal()` + `FormsModule`/`ngModel` pattern `profile` was just built with — converting only the new form would leave two form idioms coexisting for no real payoff (none of these forms have validation logic beyond "required" today). Logged as `whats-next.md` item 9: migrate all four together, one PR, later.

**Found a real gap while discussing image handling: no server-side size limit on `update-user`'s `image` field.** `/api/auth` is mounted ahead of Nest's `express.json()` in `main.ts`, so its 100KB body limit doesn't apply to better-auth's routes, and `users.image` is a plain unbounded `text` column — a bearer-token holder can currently push an arbitrarily large `image` straight through `update-user`, bypassing the UI's client-side downscale entirely. Not fixed this session (client-side downscaling was kept as-is, per the day-11 "reuse better-auth, no new controller" reasoning — server-side resize would reopen that decision for no real benefit over a size cap). Logged as `whats-next.md` item 10: add a `databaseHooks.user.update.before` hook in `auth.config.ts` rejecting oversized `image` payloads.

### What we actually built

`ui/src/app/core/auth/auth.service.ts`: `Session['user']` widened with `firstName`/`lastName`/`image` (all `string | null`); new `updateProfile(firstName, lastName, image)` posts the batched `{ name, firstName, lastName, image }` payload to `update-user` (name derived via `` `${firstName} ${lastName}`.trim() ``, per day 11's original "derived one-way" decision) then calls `initSession()` to refresh the signal. `ui/src/app/features/profile/` (new): `profile.ts`/`.html`/`.css` — first/last name as required `ngModel` signals seeded from the current session in `ngOnInit`, a file input whose `change` handler runs the picked file through `downscale-image.ts` (canvas center-crop to 256×256, `toDataURL('image/jpeg', 0.8)`) and previews the result, a "Save" button wired to `updateProfile`, a "Back" link to `/`. Route added at `/profile` (`authGuard`, lazy-loaded, matching every other route). `main.html` gained an "Edit profile" link next to "Sign out". `auth.service.spec.ts` gained two `updateProfile` cases (success round-trip incl. session refresh; failure leaves the session untouched) — the success case needed an explicit `await Promise.resolve()` between flushing the `update-user` mock and asserting the follow-up `get-session` request, since `HttpTestingController` doesn't auto-advance across the microtask boundary between two chained `firstValueFrom` calls. New `e2e/profile.spec.ts` plus a generated 64×64 fixture PNG (`e2e/fixtures/avatar.png`) sign in, navigate to `/profile`, edit the last name, attach the fixture, save, and assert the real (WebKit-rendered) downscaled JPEG data URI lands in the mocked request body.

### Problems hit along the way (and the actual fixes)

Playwright's `webServer` runs under plain Node, not Bun — the initial `e2e/profile.spec.ts` used `import.meta.url` + `fileURLToPath` to locate the fixture file (following a pattern that would work under Bun/ESM-native tooling), which Node choked on ("Cannot use 'import.meta' outside a module") because this file isn't loaded as an ES module in that context. Switched to plain `__dirname`, which every other file in `e2e/` already assumes implicitly (none of them needed a file-relative path before this one).

### Copilot review fixes

Ran `/copilot-review` against PR2 (#9) once pushed. All 5 findings it raised checked out as real, not false positives, and were fixed:

- `updateProfile` sent the trimmed `name` but raw `firstName`/`lastName` — a whitespace-only entry in either field passed HTML's `required` check yet wrote an effectively-blank name. Now trims both and throws before sending if either is empty post-trim (`auth.service.ts`).
- "Edit profile"/"Sign out" in `main.html` were nested inside `@if (household.household(); as h)`. `GET /household` 404s when no household is configured (`household.controller.ts`), and `HouseholdService.load()` sets the signal to `null` on any error — so in that state there was no way to reach profile or sign out at all. Moved both outside the household block, gated only on `auth.session()`.
- `downscaleImage`'s `img.onload` callback wasn't wrapped in `try/catch` — a throw from `drawImage`/`toDataURL` (e.g. a malformed image) would leave the returned promise pending forever and leak the object URL, since a throw inside an event-handler callback doesn't reject a promise whose executor already returned. Wrapped in `try/catch/finally`.
- The avatar `<input type="file">` had no accessible name. Added `aria-label="Choose avatar image"`.
- `save()`'s Save button was only disabled by `busy()`, not while `onFileSelected`'s async downscale was still running — selecting a large image and immediately hitting Save could submit the stale avatar. Added a `converting` signal, set for the duration of the conversion, and disabled Save on `busy() || converting()`.

Added one new unit test (`updateProfile` rejects whitespace-only names without sending a request) alongside the fixes; the rest were covered by existing tests. All 5 review threads resolved via the GraphQL API after the fixes landed and tests passed.

### Current state

`ui format:check`/`build`/`test`/`test:e2e` all green (8/8 unit, 7/7 e2e). Committed across two commits (profile page + Copilot-review fixes) and pushed to PR2 (#9), open. PR4 (chat frontend, depends on PR3, already merged) is next up.
